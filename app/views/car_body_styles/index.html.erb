<h1>Listing car_body_styles</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Wikipedia url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_body_styles.each do |car_body_style| %>
  <tr>
    <td><%= car_body_style.name %></td>
    <td><%= car_body_style.wikipedia_url %></td>
    <td><%= link_to 'Show', car_body_style %></td>
    <td><%= link_to 'Edit', edit_car_body_style_path(car_body_style) %></td>
    <td><%= link_to 'Destroy', car_body_style, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car body style', new_car_body_style_path %>
